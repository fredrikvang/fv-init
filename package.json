{
  "name": "@fredrikvang/fv-init",
  "version": "0.0.1",
  "description": "Asynchronously initialize logging, secrets, config, etc. observing dependencies. Make them easily available to the program.",
  "exports": {
    "import": "./dist/mjs/index.js",
    "require": "./dist/cjs/index.js"
  },
  "main": "dist/cjs/index.js",
  "module": "dist/mjs/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig-mjs.json && tsc -p tsconfig-cjs.json"
  },
  "author": "Fredrik Vang",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.10",
    "@types/toposort": "^2.0.3",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "toposort": "^2.0.2"
  }
}
